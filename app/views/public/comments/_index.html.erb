<div>
  <p>コメント件数：<%= post_plant.comments.count %></p>
  <% post_plant.comments.each do |comment| %>
    <p>画像</p>
    <%= comment.user.name %>
    <%= comment.created_at.strftime('%Y/%m/%d') %><%= comment.comment %>
  <% end %>
</div>